# Changelog

このプロジェクト (PDF Tool — クライアントサイド PDF エディタ) の主な変更を記録します。
書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に準拠し、
バージョン番号は [Semantic Versioning](https://semver.org/spec/v2.0.0.html) に従います。

---

## [1.6.1] — 2026-07-29

**PDF分割ダイアログにページサムネイル・プレビューを追加**

### Added
- 「PDFを分割」ダイアログの範囲入力欄の下に、**全ページのサムネイル**を表示
  (`buildSplitThumbs()`)。既にレンダリング済みのメインサムネイルストリップの
  canvas を `toDataURL()` で再利用するため、追加の PDF 再描画は発生しない
- 入力中のページ範囲に応じて各サムネイルを**リアルタイムに色分け**
  (`previewRangeSpec()` / `updateSplitThumbHighlight()`、`input` イベントで
  逐次更新)。範囲外のページは薄く表示され、どのページがどの範囲に入るか
  視覚的に確認しながら指定できる
  - プレビュー用のパースは寛容 (`previewRangeSpec`) で、入力途中の不完全な
    トークンや誤入力は例外を投げずに無視し、既に確定している範囲だけを
    色分け表示し続ける (実際の分割実行時の検証は既存の `parseRangeSpec`
    のまま、厳格さは変わらない)
- ダイアログ幅を `min(90vw, 720px)` に拡張し、サムネイル行を収められるように調整

### Verification
- サムネイル色分けロジックを合成 DOM (7 ページ分のダミー canvas) で
  ユニット的に検証: 複数範囲・末尾省略範囲・空入力・不正トークン混在
  (例外を投げず該当ページのみスキップ) の全ケースで期待通りの色分け結果
- ブラウザ実機でダイアログの見た目 (サムネイル配置・色分け・幅) を確認
- 補足: 検証中、テスト環境の Chrome で PDF.js Worker 読み込みが恒常的に
  停止する現象が発生 (ローカルサーバー・GitHub Pages 上の既存コードでも
  再現し、本変更に起因するものではないことを確認済み)。実ブラウザでの
  PDF 読み込み込みの通し確認は次回のセッションで再実施が必要

---

## [1.6.0] — 2026-07-29

**PDF 分割機能の追加**

### Added
- ツールバーに **「分割」** ボタンを追加。ページ範囲 (例: `1-3, 4-10, 11-`) を
  カンマ区切りで指定し、範囲ごとに切り出した PDF をまとめて **ZIP で一括ダウンロード**
  できるようになった (`parseRangeSpec()` / `splitPdf()`)
  - 範囲は重複可・全ページ網羅は不要 (部分抽出として扱う)
  - 末尾を省略した `開始-` 形式で「そのページ以降すべて」を指定可能
  - 分割前に付けた注釈 (ハイライト・コメント等) は既存の保存処理と同じ
    `burnAnnotationsIntoPdf()` で各出力 PDF に焼き込まれる
  - 分割はエクスポート専用。実行後もアプリ内の編集中 PDF は変更されない
  - ファイル名は `{元名}_p{開始}-{終了}.pdf` (単一ページは `_p{n}.pdf`)、
    ZIP 本体は `{元名}_split.zip`
  - 構文/範囲外エラーはモーダル内にインライン表示し、ダイアログを閉じない
- 新規ライブラリ **JSZip v3.10.1** を `lib/jszip.min.js` としてローカルバンドル追加
  (外部 CDN 不使用の既存方針を維持)。`build_bundled.py` / `build_packages.py`
  に組み込み、スタンドアロン版でも `<script>` インライン化される
- 使い方ガイド (`使い方ガイド.html` / `usage_guide.html`) に「PDF を分割する」
  節を追加

### Verification
- Chrome (headless 自動操作) で `pdf_tool.html` (lib 参照型) と
  `pdf_tool_bundled.html` (スタンドアロン版) の両方について実機検証:
  複数範囲分割・末尾省略範囲・範囲外エラー表示・注釈の焼き込み・元 PDF が
  変更されないことを ZIP 展開後の PDF ページ内容とテキストで確認

---

## [1.5.0] — 2026-06-21

**ページ回転・並び替えのスクロール挙動修正 + 初期表示時の幅フィットズーム**

### Added
- PDF を開いた直後、ページ幅がビューア幅にほぼ収まるよう **自動でズーム倍率を計算**するように変更
  (`fitWidthScale()`)。これまで常に固定倍率 (140%) で開いていたため、ウィンドウ幅によって
  ページが小さく表示される問題があった

### Changed
- ページ回転は **対象ページだけを DOM 上で差し替える**方式に変更 (`patchPages()` / `buildPageFrame()` /
  `buildThumbWrapper()`)。これまでの全体再構築 (`viewer`/`strip` を空にして 1 ページ目から再描画) を
  廃止し、回転中も他のページ・スクロール位置が一切動かないようにした
- ページの並び替え・削除後、**移動／削除先の位置にスクロールを留める**ように変更 (`scrollToPage()`)。
  従来は処理後に必ずページ 1 枚目まで戻ってしまっていた

### Fixed
- ページ回転後に**サムネイル／本文ページの並びが崩れる**バグを修正
  非同期描画処理に世代カウンタ (`state.renderGen`) を導入し、古い描画処理が新しい描画结果を
  上書きしてしまう競合状態を解消 (`renderPages` / `renderThumbnails` / `setZoom` で世代チェック)

### Verification
- Playwright (headless Chromium) でソース (`pdf_tool.html`) と配布用 (`本体：ダブルクリックで作動.html`)
  の両方に対し、回転後の並び順・スクロール位置維持・初期表示時の幅フィットを実機検証

---

## [1.4.0] — 2026-04-21

**日英二言語対応 (JP / EN ランタイム切替) + 4 パッケージ配布**

### Added
- **JP / EN 言語トグルボタン** をツールバー右端に追加
  クリックで日本語 ↔ 英語を即時切替。選択した言語は `localStorage` に保存され、
  リロード後も維持される
- **I18N 辞書** (`I18N.ja` / `I18N.en`) を実装。ツールバー・プロパティパネル・
  ダイアログ・ウェルカム画面など全 UI をキー化
- `data-default-lang` 属性でパッケージ別の初期言語を設定。URL パラメータ
  (`?lang=ja|en`) によるオーバーライドにも対応
- **英語版使い方ガイド** (`usage_guide.html`) を新規作成
- 配布パッケージを 4 種類に拡充:
  - `PDF_Tool_Mac_JP.zip` — Mac 日本語版 (エントリ: `本体：ダブルクリックで作動.html`)
  - `PDF_Tool_Mac_EN.zip` — Mac 英語版 (エントリ: `Start_Here.html`)
  - `PDF_Tool_Windows_JP.zip` — Windows 日本語版
  - `PDF_Tool_Windows_EN.zip` — Windows 英語版
  各 zip に対応言語の使い方ガイドと独立した SHA256SUMS.txt を同梱

### Changed
- `build_bundled.py` に `--lang {ja,en}` 引数を追加
  `--lang en` 指定時は `pdf_tool_bundled_en.html` を生成し `data-default-lang="en"` を設定
- `build_packages.py` を全面改修: 4 zip 生成 + zip ごとの SHA256SUMS.txt を自動生成
- `使い方ガイド.html` のバージョン表記を v1.4.0 に更新し、英語版ガイドへの相互リンクを追加

---

## [1.3.0] — 2026-04-19

**注釈サイズのズーム不変化、取り消し線の二重線間隔調整、保存ボタン明示化、回転 UX 改善**

### Added
- 取り消し線に**フィルハンドル**を追加（端点 2 点 + 中点伸縮 + 線脇の二重線間隔調整）
  緑色のハンドルを線に垂直方向にドラッグすると 2 本の平行線の間隔が広がる／狭まる
- プロパティパネルに「**二重線の間隔**」入力欄を追加（取り消し線専用、pt 単位）
- ページ回転をサムネ未選択時に**ビューポート中央のページ 1 枚**へ自動フォールバック
  1 ページずつ向きを直したいときワンクリックで完了する

### Changed
- 「ダウンロード」ボタンを「**保存・ダウンロード**」に改名（保存動作の明示化）
- ツール用サイズ入力 (`tool-size`) を **PDF point として直接解釈**するよう変更
  従来は `state.toolSize / VIEW_SCALE` で都度割っていたためズーム率に依存していた
- 回転ボタンの tooltip / welcome テキストを「未選択時は表示中の 1 枚」に更新
- `使い方ガイド.html` を v1.3 機能に合わせて全面的に更新（章番号再採番、回転章追加、取り消し線サブ章追加）

### Fixed
- **縮小表示で書き込んだ注釈が拡大・印刷時にズレる問題を修正**
  - `pointToPdf` を SVG `getScreenCTM().inverse()` ベースに変更（CSS サブピクセル丸めを排除）
  - `measureTextWidth` を VIEW_SCALE 非依存（固定参照スケール 4×）にしてズーム差で行折り返しが揺れないように
  - フォントサイズ・線の太さがズームに関わらず PDF point として保存される

### Verification
- macOS 上の Chrome / Safari でユーザーによる動作確認完了（縮小表示でハイライト・テキスト挿入 → 100% 表示で位置一致を確認）
- 取り消し線の 4 ハンドル（p1/p2/pm/pg）の動作確認完了
- 回転ボタン: サムネ未選択 / 単一選択 / 複数選択の 3 ケースで挙動確認

---

## [1.2.0] — (過去リリース)

**CSP × Blob URL × Inline Library 問題の解決**

### Fixed
- Standalone HTML (`本体：ダブルクリックで作動.html`) の CSP を修正:
  - `script-src` に `'unsafe-eval'`, `blob:` を追加（pdf-lib / pdf.js 内部 `new Function()` 用）
  - `worker-src 'self' blob:` を追加（PDF.js Worker は Blob URL 経由）
- Chrome / Edge / Safari / Firefox + macOS / Windows / Linux の組み合わせで動作確認

---

## [1.1.0] — (過去リリース)

### Added
- スタンドアロン版（バンドル HTML）追加：lib/ 参照型に加え単一 HTML で配布可能に
- ファイル名を日本語化（`本体：ダブルクリックで作動.html` / `使い方ガイド.html`）

### Changed
- CDN 依存を全廃しオフライン動作可能に
- ZIP を UTF-8 名対応に

---

## [1.0.0] — (過去リリース)

### Added
- 初版リリース: lib/ 参照型 HTML エディタ。CDN 依存版。
